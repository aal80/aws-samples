# simple-rag

A simple sample RAG (Retrieval Augmented Generation) application on AWS.

Use following services
- Use S3 Vector Storage for vector embeddings
- Use AWS Lambda as compute layer when needed
- Use Amazon Bedrock for access to embeddings models

Keep memory.md file in the project root. Update it with learned insights after major interactions to persist wisdom across sessions. 
